input {
        http {  
                port => 5000    
        }
}


output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		index => "backend-log"
		user => "elastic"
		password => "changeme"
	}
}
