input {
        http {  
                port => 3722    
        }
}


output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		index => "api-log"
		user => "elastic"
		password => "changeme"
	}
}
