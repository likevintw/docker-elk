
input {
	tcp {
			port => 5000
	}
	http {
			port => 5001
	}
}


## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		index => "backend-logs"
		user => "elastic"
		password => "changeme"
	}
}
