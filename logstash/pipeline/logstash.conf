
input {
	http {
			port => 3721
	}
}


## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		index => "backend-logs"
		user => "elastic"
		password => "changeme"
	}
}
